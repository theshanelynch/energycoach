# ESB Portal Credentials
ESB_MPRN="your_mprn_here"
ESB_EMAIL="your_email@example.com"
ESB_PASSWORD="your_password_here" # Only needed for initial login_capture

# Where to publish the JSON for Home Assistant to serve at /local
HA_WWW_DIR=../homeassistant/www

# Optional. Pin a ref for reproducibility. Leave empty to use main.
VENDOR_REF=

# Path for local vendor checkout
VENDOR_DIR=./vendor/esb-smart-meter-reading-automation
